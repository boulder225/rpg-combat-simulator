---
phase: 01-foundation-core-combat
plan: 05
type: execute
wave: 2
depends_on: ["01-04"]
files_modified:
  - src/simulation/simulator.py
  - src/agents/heuristic.py
  - run.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "DM can run a single combat simulation with heuristic agents"
    - "Combat ends with a winner declared"
  artifacts:
    - path: "src/simulation/simulator.py"
      provides: "Initiative, turn order, action execution"
    - path: "run.py"
      provides: "Usable CLI entry point"
  key_links:
    - from: "HeuristicAgent"
      to: "Simulator"
      via: "choose_action -> execute"
      pattern: "choose_action"
---

<objective>
Wire agents, simulator loop, and CLI for real combat execution.

Purpose: Enable end-to-end combat with initiative and actions.
Output: Runnable combat simulation.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Add initiative and turn-based loop</name>
  <files>src/simulation/simulator.py</files>
  <action>
    Implement initiative rolls, sorted turn order, per-round loop calling agent actions and rules until one side reaches 0 HP.
  </action>
  <verify>python run.py --dry-run || true</verify>
  <done>Turns execute in initiative order with actions applied</done>
</task>

<task type="auto">
  <name>Wire heuristic agent actions</name>
  <files>src/agents/heuristic.py</files>
  <action>
    Ensure choose_action outputs are executed by simulator (attack, move, end turn). Avoid ignoring action types.
  </action>
  <verify>python run.py --dry-run || true</verify>
  <done>Agent decisions affect combat state</done>
</task>

<task type="auto">
  <name>Replace dummy CLI with real run</name>
  <files>run.py</files>
  <action>
    Remove dummy creatures; load markdown-defined creatures, start simulator, print combat log.
  </action>
  <verify>python run.py creatures/a.md creatures/b.md || true</verify>
  <done>CLI runs a full combat and declares winner</done>
</task>

</tasks>

<verification>
End-to-end run shows initiative, actions, and winner.
</verification>

<success_criteria>
Phase 1 core combat truths now verifiable.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-combat/01-05-SUMMARY.md`
</output>
